/**
 * Factories for realtime modules implemented by business GraphQL resources.
 * Add entries here when a business endpoint gains a local `ws.ts` module.
 */
export type BusinessGraphqlWsModuleFactories = Record<never, never>;
