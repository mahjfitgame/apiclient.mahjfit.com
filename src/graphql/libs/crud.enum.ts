export enum RecordSortDirectionEnum {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum RecordSortNullPositionEnum {
  FIRST = 'FIRST',
  LAST = 'LAST',
}

export enum YesNoEnum {
  YES = 1,
  NO = 0,
}

export enum FileRelocationTypeEnum {
  MOVE = 'MOVE',
  DUPLICATE = 'DUPLICATE'
}

export enum UpsertStatusEnum {
    INSERTED = 'INSERTED',
    UPDATED = 'UPDATED',
    NOCHANGE = 'NOCHANGE',
    UNDEFINED = 'UNDEFINED'
}